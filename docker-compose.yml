version: "2"

services:
    postgres:
        extends:
            file: ./containers/base.yml
            service: postgres

    splash:
        extends:
            file: ./containers/base.yml
            service: splash

    django:
        extends:
            file: ./containers/base.yml
            service: django
